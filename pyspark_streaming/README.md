this is a small project where I created a volume and to upload json files, simulating a streaming process.
In pyspark we explore the way to flat the json files and after that, with that transformation logic, we read the data as a stream data with the use of checkpoints and archiving folders.
