this is a small project where we created a volume and  upload json files simulating a streaming process.
In pyspark we explore the way to flat the json files and after that, with that transformation logic, we read the data as a stream with the use of checkpoints and archiving folders.
